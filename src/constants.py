"""
프로젝트 전역 상수

LLM 모델, 타임아웃, 로그 설정 등을 정의합니다.
"""
from pathlib import Path

# ============================================================
# 프로젝트 경로
# ============================================================

# 프로젝트 루트 디렉토리 (이 파일 기준으로 상위 2개 디렉토리)
PROJECT_ROOT = Path(__file__).parent.parent.absolute()

# ============================================================
# LLM 모델 설정
# ============================================================

# 메인 에이전트 모델 (Claude Sonnet 4)
# 코딩, GitHub, Bash, 오류 분석 등 모든 핵심 작업 수행
MAIN_AGENT_MODEL = "claude-sonnet-4-20250514"

# 로거 에이전트 모델 (Claude Haiku 4)
# 작업 로그 생성 전용 (비용 효율적)
LOGGER_AGENT_MODEL = "claude-haiku-4-20250514"


# ============================================================
# 작업 타임아웃 설정
# ============================================================

# 단일 작업 타임아웃 (분)
TASK_TIMEOUT_MINUTES = 30

# Bash 명령 기본 타임아웃 (초)
BASH_TIMEOUT_SECONDS = 120


# ============================================================
# 로그 설정
# ============================================================

# 로그 디렉토리 경로
LOG_DIRECTORY = ".ccw/logs"

# 컨텍스트 로드 시 읽을 최근 로그 개수
MAX_CONTEXT_LOGS = 2

# 로그당 최대 토큰 수 (대략 4글자 = 1토큰)
LOG_TOKEN_LIMIT = 2000

# 로그 최대 글자 수
LOG_MAX_CHARS = LOG_TOKEN_LIMIT * 4  # 8000글자


# ============================================================
# 파일 경로 설정
# ============================================================

# 애플리케이션 로그 파일 경로
APP_LOG_FILE = ".ccw/app.log"

# 환경 변수 파일 경로
ENV_FILE = ".env"


# ============================================================
# 명령어 블랙리스트 (보안)
# ============================================================

# 파괴적 명령어
DESTRUCTIVE_COMMANDS = [
    "rm",
    "rmdir",
    "mkfs",
    "dd",
    "format",
    "fdisk",
    "parted",
    ":(){:|:&};:",  # Fork bomb
]

# 대화형 명령어 (지원 불가)
INTERACTIVE_COMMANDS = [
    "vi",
    "vim",
    "nvim",
    "nano",
    "emacs",
    "less",
    "more",
    "top",
    "htop",
    "sudo",
]

# 보호된 시스템 경로 (쓰기 차단)
PROTECTED_PATHS = [
    "/etc/",
    "/sys/",
    "/proc/",
    "/boot/",
    "/dev/",
]


# ============================================================
# API 설정
# ============================================================

# Claude API 최대 토큰 수
MAX_TOKENS = 4096

# API 재시도 횟수
API_RETRY_COUNT = 3

# API 재시도 대기 시간 (초)
API_RETRY_DELAY_SECONDS = 5
